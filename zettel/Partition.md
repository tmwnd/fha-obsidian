Sei $[0, \overline{T}]$ eine Menge an Zeitpunkten.

Eine Partition $\tau$ von $[0, \overline{T}]$ ist definiert als

$$
	\tau := (t_i)_{i \in \{ 1, \dots, n \}}
$$

mit
- $0 = t_0 \lt t_1 \lt \dots \lt t_n = \overline{T}$

Es gilt
- $|\tau| := \sup_{i \in \{ 1, \dots, n \}} |t_i - t_{i-1}|$