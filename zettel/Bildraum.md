Sei $Ax = b$ ein [[zettel/Lineares Gleichungssystem|Lineares Gleichungssystem]].

Der *Bildraum* $R(A)$ ist definiert als

$$
	R(A) := \{ y \mid y = Ax \}
$$

Es gilt
- $Ax = b$ lösbar $\iff$ $b \in R(A)$